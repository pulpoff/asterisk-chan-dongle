;; modules.conf — Asterisk module loading configuration

[modules]
autoload=yes

; Ensure chan_dongle is loaded
load => chan_dongle.so

; Trunk protocol modules (all loaded, only the configured one is used)
load => chan_iax2.so
load => chan_sip.so
load => chan_pjsip.so

; SRTP
load => res_srtp.so
